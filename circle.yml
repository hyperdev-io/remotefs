machine:
  services:
    - docker

dependencies:
  override:
    - echo "no install"

test:
  override:
    - echo "no tests!"

deployment:
  hub:
    tag: /^[0-9]+\.[0-9]+\.[0-9]+$/
    commands:
      - docker build -t ictu/remotefs:${CIRCLE_TAG} .
      - MAJOR_VERSION=${CIRCLE_TAG/.*}; docker tag ictu/remotefs:${CIRCLE_TAG} ictu/remotefs:${MAJOR_VERSION}
      - MINOR_VERSION=${CIRCLE_TAG%.*}; docker tag ictu/remotefs:${CIRCLE_TAG} ictu/remotefs:${MINOR_VERSION}
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push ictu/remotefs:${CIRCLE_TAG}
      - docker push ictu/remotefs:${MINOR_VERSION}
      - docker push ictu/remotefs:${MAJOR_VERSION}
